# Codebase Documentation

This folder contains implementation-level docs for how key user actions map to frontend and backend execution paths.

## Available Docs

1. `start-recording-flow.md`
- What happens under the hood when the user clicks **Start Recording**.
- Includes frontend event path, WebSocket message flow, backend orchestration, model pipeline stages, and edge cases.

